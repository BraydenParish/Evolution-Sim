# AGENTS
- Follow repository workflow: plan before changes, add tests first, implement minimal diff, run tests.
- Default commands: tests `<TEST_CMD>`, type check `<TYPECHECK_CMD or none>`, lint `<LINT_CMD or none>`, security `<SECURITY_CMD or none>`.
- Preserve critical invariants and avoid new dependencies.
